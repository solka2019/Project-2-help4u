var taskModel = require("../models/task");
// framework to test
// describe("(Test Suite) All my Employee class tests", () => {
  test("Dummy test", () => {
    var test = 1;
    expect(test).toBe(1);
  });
